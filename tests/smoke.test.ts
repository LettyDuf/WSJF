// Test conservé volontairement vide (suppression bloquée par les permissions du dossier monté).
import { describe, it, expect } from 'vitest';
describe('placeholder', () => {
  it('passe', () => { expect(true).toBe(true); });
});
